<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MobileNote" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/mainColor</item>
        <!-- <item name="colorPrimaryContainer">@color/mainColor</item>
        <item name="colorSecondary">@color/my_secondary</item>
        <item name="colorSecondaryContainer">@color/my_secondary_container</item>
        <item name="colorBackground">@color/my_background</item>
        <item name="colorSurface">@color/my_surface</item>
        <item name="colorOnPrimary">@color/my_on_primary</item> -->
    </style>

    <style name="SplashTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/mainColor</item>
        <item name="android:windowBackground">@color/mainColor</item>
    </style>

    <style name="Theme.MobileNote" parent="Base.Theme.MobileNote" />
</resources>